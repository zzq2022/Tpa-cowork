node_modules
.vite-cache
dist/
.DS_Store
*.env
.env*

# This project uses pnpm; reject stray npm/yarn lockfiles
package-lock.json
yarn.lock

# Rust / Tauri
target/
src-tauri/target/
src-tauri/WixTools/
src-tauri/resources/vc_redist.x64.exe
src-tauri/resources/browser-host/
src-tauri/resources/chrome-extension/
# Eval sidecar staged by scripts/prepare-eval-sidecar.mjs (a ~200MB locally
# built binary, same class as the browser-host above — never committed).
src-tauri/binaries/

# Agent Python runtime ZIP staged by scripts/prepare-zip-venv.mjs
# (~60MB+ prebuilt; never commit — local/CI packaging artifact).
agent-venv/
agent-venv.zip

# Python (skill-creator scripts, eval helpers)
__pycache__/
*.pyc
*.pyo

# Hope Agent runtime data
.hope-agent/

# IDE
.vscode/
.idea/
*.swp
tool_comparison.md

# Private research
docs/research/

# Local requirements / PRD drafts
docs/requirements/
docs/plan/*
!docs/plan/memory-next-generation-plan.md

# Local assistant state（worktrees / settings.local.json 等）
.claude/

# Temporary build & scratch artifacts
.scratch/*
!.scratch/tpa-cowork-customization-spec.md
pack.md

